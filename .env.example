# SMTP Email Configuration for Password Reset
# Copy this file to .env and fill in your SMTP credentials

# SMTP Server Host (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.gmail.com

# SMTP Port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP Username (your email address)
SMTP_USERNAME=your-email@example.com

# SMTP Password (or app-specific password for Gmail)
SMTP_PASSWORD=your-password-here

# From Email Address (shown in recipient's inbox)
SMTP_FROM_EMAIL=noreply@jpbdselangor.gov.my

# From Name (shown in recipient's inbox)
SMTP_FROM_NAME=PLAN Malaysia Selangor Helpdesk

# Instructions for Gmail:
# 1. Enable 2-Factor Authentication in your Google Account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the App Password as SMTP_PASSWORD
